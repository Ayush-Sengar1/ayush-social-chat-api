# Social Media and Chat Application

Features: React frontend, Express API, register/login with bcrypt and JWT, Socket.io instant messaging, responsive layout, and in-memory demo data so it runs without database setup.

Run npm install. In terminal 1 run npm run server. In terminal 2 run npm run client. Open http://localhost:5173.
